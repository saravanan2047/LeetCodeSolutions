class Solution 
{
    public boolean isPowerOfThree(int n) 
    {
       for (int i = 0; i < 31; i++) 
       {
            int ans = (int) Math.pow(3, i);
            if (ans == n && n!=Integer.MAX_VALUE) 
            {
                return true;
            }
        }
        return false;
        
    }
}

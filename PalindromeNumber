class Solution {
    public boolean isPalindrome(int x) 
    {
        int res=0;
        int m=x;
        while(x>0)
        {
            int temp=x%10;
            res=res*10+temp;
            x=x/10;
        }
        return res==m?true:false;
        
    }
}
